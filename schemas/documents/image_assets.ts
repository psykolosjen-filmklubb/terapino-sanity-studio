import { defineField, defineType } from "sanity";

export default defineType({
  name: "image_assets",
  title: "Image Assets",
  type: "document",
  fields: [
    defineField({
      name: "name",
      title: "Name",
      type: "string",
    }),
    defineField({
      name: "image",
      title: "Image",
      type: "image",
      options: {
        hotspot: true,
      },
    }),
  ],
});
